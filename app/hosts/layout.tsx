import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Bitcoin Mining Hosting Companies 2026',
  description: 'Compare Bitcoin mining hosting companies. Find the best hosting rates, locations, and contracts.',
}

export default function HostsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
